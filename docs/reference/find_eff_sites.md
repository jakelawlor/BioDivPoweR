# Find effective number of sites needed to reach target coverage

Find effective number of sites needed to reach target coverage

## Usage

``` r
find_eff_sites(coverage_vec, target = c(1))
```

## Arguments

- coverage_vec:

  a vector of coverage values

- target:

  coverage value to achieve with n sites

## Value

number of sites necessary to achieve a target coverage value
